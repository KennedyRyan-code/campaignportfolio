import React from "react";

const Section = ({
  className,
  id,
  crosses,
  crossesOffset,
  customPaddings,
  children,
}) => {
  return <div className={``}>{children}</div>;
};

export default Section;
